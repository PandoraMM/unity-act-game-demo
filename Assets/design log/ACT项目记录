总结：我感觉在这个阶段，动作游戏的基础设计对我来说又简单又难。简单的是时间，所有问题都可以在时间维度上去解决，比如阶段划分、判定生效时间、攻击突进生效时间等；但是难得部分也在于时间，就是如果在一个单维度的概念中塞下大量的“不同作用的判定方式”比如攻击阶段和各种窗口期：



接下来具体了解一下代码的各个函数，是时候该进行一个总结了，既然时间是重要指标，那就从时间入手：如何获得时间，或者说获得什么时间，这里我理解有两个方向：1.玩家的输入；2.角色的动画播放；

1.玩家的输入：
当玩家按下攻击之后，开始进行输入计时，然后，攻击请求AttackRequest会发生于各个可以进行攻击的状态中（目前我只有两个，即GroundState和AttackState），在AttackRequest中会进行“挂帐”和“attack input buffer”的处理，在各个状态中会对攻击讯号进行消费，以防止重复触发。

2.角色的动画播放：
2-1.动画的播放，在Player类中的PlayerAnimation(int animName, int animLayer = 0)函数针对目标动画的名字和层级和速度进行播放，针对攻击状态，用于在进入攻击状态和切换攻击段数的时候进行播放（这里针对这个函数又进一步进行了数据处理，后面针对数据部分会再说）
2-2.动画的信息比对和归一化时间的获取，这是一个非常核心的函数，暂且称为“伟大函数”！在Player类中的TryGetNormalizedTimeOfAnimation(int animName, out float normalizedTime, int animLayer = 0)，这个函数主要的作用就是在比对动画层级和动画名字都正确的时候，还会返回当前动画的归一化时间，如此就可以找到当前动画的每一个时间点进行数据编辑和获取
2-3.动画的播放完结，在Player类中的IsAnimationComplete(int animName, int animLayer = 0)，这个函数同样利用了“伟大函数”中针对归一化时间的部分，即如果当前动画的播放时间>= 0.98（这是一个没有那么魔法的魔法数字）就认为动画播放完成




然后是如何在这些时间上“跳舞”，这里我理解就是，所谓的“舞蹈”，就是数据，依托于时间的数据！这里可以分为两个维度，或者说两种规划方式：1.阶段；2.窗口

1.阶段：
一段攻击可分为：攻击前摇、攻击中、攻击后摇，以此来划分一些比较大块的切换条件。针对我的代码：
1-1.主要是获取攻击阶段GetCurrentAttackStage();
1-2.判断是否能进行连击操作CanInputNextCombo()（这里面包括函数CheckAttackStageWindowByTime()并且他才是核心，利用“伟大函数”获得归一化时间、创建了攻击阶段的比对、窗口结束时间的比对），而连击操作又依托于数据中的连击窗口时间；
1-3.动作的打断，通过针对玩家意图和攻击状态中的制定阶段的判断是否可以进行打断，即函数CanBeInterruptedNow(StateIntention intentionType , AttackStage requiredStage)，而意图则是通过函数GetAttackInterrupted(StateIntention intentionType)进行获取，但是依旧是利用数据进行比对，即如果数据中的意图希望被打断，就会返回意图类型，然后在函数CanBeInterruptedNow进行进一步的对比，比如“跳跃意图可以打断攻击阶段的后摇阶段”

2.窗口：这里就是对整个动画时间的各个时间点，注意我说的是时间点而不是阶段！进行明确的划分！
attackMoveStartWindow;//攻击位移开始时间，攻击动作中从这个时间点开始会有攻击位移的效果
attackMoveEndWindow;  //攻击位移结束时间，攻击动作中从这个时间点开始攻击位移的效果结束，恢复到正常移动的状态
attackMoveSpeed;//攻击位移的速度，在攻击位移的时间窗口内，角色会以这个速度进行移动，通常是一个较高的速度，用来表现攻击动作中的冲刺或者推进效果，增加攻击的打击感和流畅度
public float hitStartWindow;//攻击判定开始时间，攻击动作中从这个时间点开始会有攻击判定的效果，通常是一个较早的时间点，用来表现攻击动作中的挥出武器或者身体的一瞬间，这个时间点之后敌人如果进入攻击判定范围就会被判定为命中
hitEndWindow;//攻击判定结束时间，攻击动作中从这个时间点开始攻击判定的效果结束，通常是一个较晚的时间点，用来表现攻击动作中的收回武器或者身体的一瞬间，这个时间点之后敌人如果进入攻击判定范围就不会被判定为命中
hitStopDuration;      //主角击中停顿的持续时间，在这个时间内可以实现击中停顿效果，增加打击感等
enemyHitStopDuration; //敌人被击停顿的持续时间，在这个时间内敌人会进入击中停顿状态，增加打击感等
这些窗口就是“数据”，也就是所谓的“舞蹈”



接着是怎么处理这些数据：这时候就要利用刷新逻辑的方式了，有几种：进入状态的Enter函数；数据逻辑在Update中进行处理；物理逻辑在FixedUpdate中进行处理

1.Enter：
作为数据也好，表现也好的初始化函数，但是我感觉针对动作游戏，我把它理解为进入更合适，比如一旦接收到了来自其他状态的攻击请求且请求成功，就要即可切换到这个状态中进行动画播放和攻击段数的初始化。

2.Update：
实时获取攻击的各个阶段、针对是否可以进行combo的检测（这里又和输入联系起来了，所以又回到了曾经的攻击请求部分；而且要去处理是不是能进行combo的判断逻辑，当然播放动画也要在这里再进行）、状态打断，比如在攻击状态下玩家输入了其他状态的讯号（跳跃），那能否满足打断意图？、状态完成，当动画播放完成之后状态切换

3.FixedUpdate：
这里其实就相对的简单，主要处理的就是“舞蹈”的表现部分，并且还是涉及到物理的部分，比如HitStop、HitMove... ...我大胆假设一下，如果我要做“风雷震落”说不定也会把物理表演放在这里！




最后就是对于数据的处理，生动点就是“舞蹈的更有节奏”，这时就添加了真正的表演的部分！比如attackMove、hitStop、Hit当然这些部分就不做展开了，我会体现在后续的文档中。

所以请帮我判断一下，我的这个理解方式是不是对的，最起码大的结构是否清晰